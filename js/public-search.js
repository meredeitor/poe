import{configured}from"./firebase.js";import{listPublished}from"./poe-service.js";import{esc,registerSW}from"./utils.js";const results=document.querySelector("#results"),count=document.querySelector("#count"),input=document.querySelector("#search");let poes=[];const render=q=>{const term=q.trim().toLowerCase(),shown=poes.filter(p=>!term||[p.codigo,p.nombre,p.area,p.proceso,p.maquina].some(v=>String(v||"").toLowerCase().includes(term)));count.textContent=`${shown.length} disponibles`;results.innerHTML=shown.length?shown.map(p=>`<a class="poe-card" href="poe.html?id=${encodeURIComponent(p.id)}"><span class="code">${esc(p.codigo)}</span><h3>${esc(p.nombre)}</h3><p>${esc(p.area||"Sin área")} · ${esc(p.maquina||"Sin máquina")}</p><div class="poe-meta"><span class="badge">VIGENTE</span><strong>Consultar →</strong></div></a>`).join(""):`<div class="empty">No se encontraron procedimientos vigentes.</div>`};document.querySelector("#search-form").addEventListener("submit",e=>{e.preventDefault();render(input.value)});input.addEventListener("input",()=>render(input.value));if(configured){listPublished().then(x=>{poes=x;render("")}).catch(()=>results.innerHTML='<div class="empty">No fue posible cargar los procedimientos. Verifica tu conexión.</div>')}else results.innerHTML='<div class="empty">Conecta el proyecto Firebase para consultar los POE publicados.</div>';registerSW();
