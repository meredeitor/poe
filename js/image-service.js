const TYPES=["image/jpeg","image/png","image/webp"],MAX_SOURCE=8*1024*1024,MAX_DATA_URL=700000;
const canvasToDataUrl=(canvas,quality)=>canvas.toDataURL("image/webp",quality);
export async function prepareImage(file){if(!TYPES.includes(file.type))throw Error("Formato de imagen no permitido.");if(file.size>MAX_SOURCE)throw Error("La fotografía original supera 8 MB.");const bitmap=await createImageBitmap(file);let scale=Math.min(1,1280/Math.max(bitmap.width,bitmap.height));for(let resize=0;resize<4;resize++){const canvas=document.createElement("canvas");canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));canvas.getContext("2d",{alpha:false}).drawImage(bitmap,0,0,canvas.width,canvas.height);for(let quality=.78;quality>=.42;quality-=.08){const dataUrl=canvasToDataUrl(canvas,quality);if(dataUrl.length<=MAX_DATA_URL)return{dataUrl,mimeType:"image/webp",bytes:Math.ceil((dataUrl.length-23)*.75),width:canvas.width,height:canvas.height}}scale*=.78}throw Error("No fue posible reducir la fotografía. Usa una imagen más sencilla.")}
